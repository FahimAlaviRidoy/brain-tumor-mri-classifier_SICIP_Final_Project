Place the following screenshots here after running training, evaluation,
and the Docker app (referenced throughout README.md and final_report.md):

  - mlflow_runs.png        : MLflow experiment list showing all 4 model runs
  - training_result.png    : A run's detail page (params + metric charts)
  - docker_app_running.png : Terminal output of `docker run` succeeding
  - demo_output.png        : Streamlit UI showing an uploaded MRI + prediction

You can delete this file once real screenshots are added.
